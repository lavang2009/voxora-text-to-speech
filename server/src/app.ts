import express from "express"; import cors from "cors"; import helmet from "helmet"; import rateLimit from "express-rate-limit"; import {env} from "./config/env.js"; import voiceRoutes from "./routes/voiceRoutes.js"; import ttsRoutes from "./routes/ttsRoutes.js"; import uploadRoutes from "./routes/uploadRoutes.js"; import {errorHandler} from "./middleware/errorHandler.js";
const app=express();app.use(helmet());app.use(cors({origin:env.CLIENT_ORIGIN}));app.use(express.json({limit:"1mb"}));app.use(rateLimit({windowMs:60000,limit:30}));app.get("/api/health",(_q,r)=>r.json({success:true,data:{status:"ok"}}));app.use("/api/voices",voiceRoutes);app.use("/api/tts",ttsRoutes);app.use("/api/upload",uploadRoutes);app.use(errorHandler);export default app;