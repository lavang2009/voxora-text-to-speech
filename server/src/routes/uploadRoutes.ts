import {Router} from "express"; import multer from "multer"; import {requireAuth} from "../middleware/auth.js"; import {uploadAvatar} from "../controllers/uploadController.js"; const r=Router();const up=multer({storage:multer.memoryStorage(),limits:{fileSize:5*1024*1024},fileFilter:(_q,f,cb)=>cb(null,["image/jpeg","image/png","image/webp"].includes(f.mimetype))});r.post("/avatar",requireAuth,up.single("file"),uploadAvatar);export default r;