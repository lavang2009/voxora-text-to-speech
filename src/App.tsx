import {useEffect,useState} from "react"; import {BrowserRouter,Routes,Route,Navigate} from "react-router-dom"; import {subscribeAuth,appUser} from "./services/authService"; import {useAppStore} from "./store/useAppStore"; import AppLayout from "./layouts/AppLayout"; import Navbar from "./components/Navbar"; import Landing from "./pages/Landing"; import Auth from "./pages/Auth"; import ForgotPassword from "./pages/ForgotPassword"; import Dashboard from "./pages/Dashboard"; import Studio from "./pages/Studio"; import Voices from "./pages/Voices"; import History from "./pages/History"; import Favorites from "./pages/Favorites"; import Profile from "./pages/Profile"; import Settings from "./pages/Settings"; import NotFound from "./pages/NotFound";
function Guard(){const u=useAppStore(s=>s.user);return u?<AppLayout/>:<Navigate to="/login" replace/>}
export default function App(){const ready0=useAppStore;const [ready,setReady]=useState(false);const setUser=ready0(s=>s.setUser);useEffect(()=>subscribeAuth(u=>{setUser(u?appUser(u):null);setReady(true)}),[]);if(!ready)return <div className="grid min-h-screen place-items-center bg-[#080b14]">Loading Voxora…</div>;return <BrowserRouter><Routes><Route path="/" element={<Landing/>}/><Route path="/login" element={<Auth mode="login"/>}/><Route path="/register" element={<Auth mode="register"/>}/><Route path="/forgot-password" element={<ForgotPassword/>}/><Route path="/voices" element={<><Navbar/><div className="pt-16"><Voices/></div></>}/><Route element={<Guard/>}><Route path="/dashboard" element={<Dashboard/>}/><Route path="/studio" element={<Studio/>}/><Route path="/history" element={<History/>}/><Route path="/favorites" element={<Favorites/>}/><Route path="/settings" element={<Settings/>}/><Route path="/profile" element={<Profile/>}/></Route><Route path="*" element={<NotFound/>}/></Routes></BrowserRouter>}