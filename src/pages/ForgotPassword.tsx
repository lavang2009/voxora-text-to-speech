import {useState} from "react"; import {Link} from "react-router-dom"; import {Button,Card,Input,Label} from "../components/ui"; import {resetPassword} from "../services/authService";
export default function ForgotPassword(){const [e,setE]=useState(""),[msg,setMsg]=useState(""),[err,setErr]=useState("");return <div className="grid min-h-screen place-items-center bg-[#080b14] p-4"><Card className="w-full max-w-md p-7"><h1 className="text-2xl font-black">Reset password</h1><div className="mt-6"><Label>Email</Label><Input value={e} onChange={x=>setE(x.target.value)}/></div>{msg&&<p className="mt-3 text-sm text-emerald-300">{msg}</p>}{err&&<p className="mt-3 text-sm text-red-300">{err}</p>}<Button className="mt-5 w-full" onClick={async()=>{try{await resetPassword(e);setMsg("Reset email sent.")}catch(x:any){setErr(x?.message||"Unable to send reset email.")}}}>Send reset link</Button><Link to="/login" className="mt-4 block text-center text-sm text-violet-300">Back to sign in</Link></Card></div>}