import {Button,Card,Label} from "../components/ui"; import {useAppStore} from "../store/useAppStore"; import {savePreferences} from "../services/firestoreService";
export default function Settings(){const s=useAppStore();const save=async()=>s.user&&savePreferences(s.user.uid,s.preferences);return <div className="mx-auto max-w-3xl p-4 md:p-8"><h1 className="text-3xl font-black">Settings</h1><Card className="mt-7 space-y-5 p-6"><div><Label>Default language</Label><input value={s.preferences.defaultLanguage} onChange={e=>s.setPreferences({defaultLanguage:e.target.value})} className="w-full rounded-xl bg-white/[.04] p-3"/></div><div><Label>Default voice ID</Label><input value={s.preferences.defaultVoice} onChange={e=>s.setPreferences({defaultVoice:e.target.value})} className="w-full rounded-xl bg-white/[.04] p-3"/></div><div><Label>Theme</Label><select value={s.preferences.theme} onChange={e=>s.setPreferences({theme:e.target.value as any})} className="w-full rounded-xl bg-slate-900 p-3"><option value="system">System</option><option value="dark">Dark</option><option value="light">Light</option></select></div><div className="flex justify-end"><Button onClick={save}>Save preferences</Button></div></Card></div>}